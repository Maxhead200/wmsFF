server:
  port: ${WMS_PORT:8080}

spring:
  application:
    name: wms-logoff
  jackson:
    default-property-inclusion: non_null

management:
  endpoints:
    web:
      exposure:
        include: health,info

wms:
  demo:
    enabled: ${WMS_DEMO_SEED:true}
